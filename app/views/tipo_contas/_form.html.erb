<%= form_with(model: tipo_conta) do |form| %>
  <% if tipo_conta.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tipo_conta.errors.count, "error") %> prohibited this tipo_conta from being saved:</h2>

      <ul>
        <% tipo_conta.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
